import { ConvertHourPipe } from './convert-hour.pipe';

describe('ConvertHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertHourPipe();
    expect(pipe).toBeTruthy();
  });
});
